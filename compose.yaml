version: "3.8"
services:
  app:
    image: node:18
    environment:
      WYZE_EMAIL: ${WYZE_EMAIL:-""}
      WYZE_PASSWORD: ${WYZE_PASSWORD:-""}
      API_ID: ${API_ID:-""}
      API_KEY: ${API_KEY:-""}
